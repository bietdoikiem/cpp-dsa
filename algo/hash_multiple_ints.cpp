#include <bits/stdc++.h>

using namespace std;

typedef long long ll;

ll get_hash(int i, int j, int k, int l) {
    ll key = i + 1e2 * j + 1e4 * k + 1e6 * l;
    return key;
}

int main() {
    return 0;
}